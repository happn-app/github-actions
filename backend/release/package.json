{
  "private": true,
  "scripts": {
    "build": "rimraf dist && ncc build src/index.ts -o dist --source-map"
  },
  "dependencies": {
    "@actions/core": "^1.2.3",
    "@actions/github": "^4.0.0",
    "@slack/webhook": "^6.0.0",
    "simple-git": "^2.48.0"
  },
  "devDependencies": {
    "@types/node": "^12.20.11",
    "@vercel/ncc": "^0.28.3",
    "rimraf": "^3.0.2",
    "typescript": "^4.2.4"
  }
}
